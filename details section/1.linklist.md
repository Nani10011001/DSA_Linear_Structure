insertmethod:adding node in anywhere

step!:create a node
step2:where want to insert the element
we should want to acces before element
we want to point the node to node from before node and after node

size()
it takes head as the start then count initilily zero then 
uses while loop then its start increase the count and then point the temp.next
return the count

clear( )method  to clear the linklist