push():








pop():
frist we check if  the node is empty are not then we assign the value to the undefined
then we start from the back

temp variable to point to the tail
then move the tail to the prev back variable the we can break the chain of elements

to unshift()
frist we create new node
then we make connection  of the node to next head node
then we make connection  of the node to prev newnode
then this.head is newnode

 shift()
 store temp =this.head
 frist we want to shift this.head=this.head.next to change the next head
 then we should want break the connection of it 
 this.head.prev=null
 temp.next=null
 then we reduce the this.length--
 return the value
 

